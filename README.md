float contains the heavy version of FFT
integerized contains the lighter version of FFT with lower space occupation by variables making it more memory efficient but it tradeoffs with the accuracy of the measured value
