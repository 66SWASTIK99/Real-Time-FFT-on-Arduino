float contains the heavy version of FFT<br>
integerized contains the lighter version of FFT with lower space occupation by variables making it more memory efficient but it tradeoffs with the accuracy of the measured value<br>
plot.py is the frequency plotter for arduino as arduino sends data serially
